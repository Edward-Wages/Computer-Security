import connection
import crypto
import gui

#Main script that initializes the GUI and ties everything together.

def main():
    #Initializes the GUI

    #Initialize the network connection

    #Initialize the crypto module

    #Main event loop
